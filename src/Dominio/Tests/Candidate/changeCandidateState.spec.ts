import { Candidate } from "src/Dominio/AggRoots/Candidate/Candidate"

  
  
//   const suspendedCandidate = ()=> Candidate.suspend
//   /**
//      * Unit tests related to Candidate State
//      * */
   
   
// describe("changing a Candidate State", ()=>{
//         it('Candidate state needs to be active in order to change it to suspended',()=>{
//             const event = suspendedCandidate()
//             expect().toThrowError(InvalidCandidatePhoneNumber)
//         }),
//     })